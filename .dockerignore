# 빌드 컨텍스트를 가볍게 유지한다 — 아래 항목은 이미지 빌드에 필요 없다.
.git
.gitignore
.gradle
build
out
*.iml
.idea
.vscode
docs
*.md
.env
.env.*
!.env.example
# .dockerignore는 .gitignore와 달리 슬래시 없는 패턴이 하위 디렉터리까지 재귀 매칭되지
# 않는다(빌드 컨텍스트 루트만 매칭) — 실제 파일이 src/main/resources/ 아래 있어 **/ 접두사가
# 필요하다(리뷰 지적 P2).
**/application-local.yml
