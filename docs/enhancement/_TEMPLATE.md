# 고도화 — <도메인> (담당: <이름/미정>)

> 이 문서는 동결된 MVP 정본에 대한 **델타(변경분)**다. 아래 `전제` 절에 적힌 SA·PRD 절만 함께 읽으면 되고, 그 밖의 MVP 문서는 읽지 않는다.
> 충돌 시: 이 문서가 `override 범위`에 선언한 것 안에서는 이 문서 > 동결 SA. 범위 밖은 동결 SA가 정본. (레이어 규칙: [README](README.md))

## 메타

- **상태:** 초안 <!-- 초안 | 설계중 | 구현중 | 완료 -->
- **담당:** <1명>
- **관련 이슈:** #
- **전제(동결 baseline — AI는 이 절만 함께 읽는다):** SA §..., SA §..., PRD §...
- **override 범위(이 문서가 바꾸는 계약):** <예: notifications 수신자 모델>
- **건드리지 않는 것:** <예: 결제 알림 발행 로직은 그대로 유지>

## 배경 / 문제

<MVP에서 무엇이 비어 있거나 구멍인지. 근거는 파일:라인 또는 정본 절.>

## 설계 (델타)

<동결 baseline 대비 "무엇을 어떻게 바꾸는지"만. baseline과 같은 건 반복하지 않는다.>

## 미결정 — 구현 전 질문

<정책·설계 결정이 필요한 항목. 확정 전엔 구현하지 않는다(AGENTS 규칙).>

## 검증 계획

<STRICT 여부·필요한 테스트 Level. 판정은 PASS/FAIL/PARTIAL/BLOCKED.>
