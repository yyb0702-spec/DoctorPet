# 고도화 — 알림 (담당: @maschemy)

> 이 문서는 동결된 MVP 정본에 대한 **델타(변경분)**다. 아래 `전제` 절에 적힌 SA·PRD 절만 함께 읽으면 되고, 그 밖의 MVP 문서는 읽지 않는다.
> 충돌 시: 이 문서가 `override 범위`에 선언한 것 안에서는 이 문서 > 동결 SA. 범위 밖은 동결 SA가 정본. (레이어 규칙: [README](README.md))

## 메타

- **상태:** 구현중 (3.10 수신자 모델 구현·SA §4 승격 반영 #141, 병원 수신 SSE는 **전달 경로(transport)만** 완료 — 발행 지점 미착수)
- **담당:** @maschemy
- **관련 이슈:** #141 (3.10 수신자 모델 확장), #166 (병원 수신 알림 발행 지점 — 3.10 transport 후속)
- **전제(동결 baseline — 이 절만 함께 읽는다):** SA §9-8(폴링 + MVP2 단방향 **SSE 확정**·NotificationPusher 추상화·티켓 인증·커밋 후 전송), SA §8-8(알림 API), SA §4 notifications
- **override 범위(이 문서가 바꾸는 계약):** ① 수신자 모델(MEMBER 전용 → MEMBER/HOSPITAL), ② 미읽음 수·모두 읽음 API, ③ 채널 확장(인앱·SSE에 **이메일** 추가) + 수신 설정, ④ 저장 신뢰성(아웃박스)
- **건드리지 않는 것:** **예약 승인/거절 알림 발행은 develop에 이미 구현됨**(작업 아님). **SSE 실시간 인프라**(`NotificationPusher`·`SseNotificationPusher`·`NotificationPushListener` AFTER_COMMIT·티켓 인증·heartbeat)는 **유지·재사용**한다(버리지 않음). 결제 발행 지점·읽음 멱등 유지. **WebSocket은 채팅 전용**([채팅.md](채팅.md)) — 알림을 WebSocket으로 옮기지 않는다(전송 2계층: 알림=SSE, 채팅=WebSocket).

## develop 현황 — 이미 된 것(작업 아님)
- **예약 승인/거절/자동거절/노쇼 알림 발행: DONE.** `HospitalReservationApplicationService`가 `publishConfirmed`(L84)·`publishRejected`(L119)·`publishNoShow`(L263) 호출, `publishAutoRejected`까지 분리.
- **단방향 실시간 push: SSE로 DONE(#40).** `NotificationService.create()`가 `NotificationCreatedEvent` 발행 → `NotificationPushListener`(AFTER_COMMIT)가 `SseNotificationPusher`로 전송. 티켓 인증(`NotificationSubscriptionController`)·heartbeat·연결상한까지. → 이 채널을 **유지**한다.

## 도메인 경계 조율
- **병원 확정취소·대기열 승급 알림**(@springday3355-tech [예약.md](예약.md))은 새 `NotificationType` 값이 필요 → **알림 도메인이 정의·합의**한다(임의 확장 금지, AGENTS).
- 모든 발행은 `NotificationService.create()`를 경유한다 → 기존 SSE가 자동 전송한다.

---

## 3.8 미읽음 개수 + 모두 읽음 (고도화) — 웨이브 0, 지금 바로
**범위:** `GET /api/notifications/unread-count`(count 쿼리 신설), `PATCH /api/notifications/read-all`(조건부 bulk UPDATE, 멱등).
**근거:** 현재 컨트롤러는 GET 목록 + PATCH `/{id}/read`뿐. 배지 숫자에 목록 전체 폴링, 30개면 읽음 PATCH 30번.
**독립성:** 스키마 변경 없음, 순수 추가. 다른 결정·작업과 무관 → 착수 1순위.

## 3.10 수신자 모델 확장 — 병원 단위 공유 (홀 + 채팅 전제)
**범위:** `notifications`에 수신자 타입 도입(MEMBER | HOSPITAL). 병원향 알림은 `recipient_type=HOSPITAL, recipient_id=hospitalId`로 저장, **읽음도 병원 단위 1건**. SSE 레지스트리(`SseEmitterRegistry`)도 `memberId` 키라 병원 실시간 수신 경로가 없으므로, 이 확장이 병원 SSE 수신도 함께 연다.
**결정(확정):** 병원 수신·읽음 = **병원 단위 공유**(스태프 누구나 열람, 개인 fan-out·읽음 복제 없음). 스태프 계정 관리가 범위 밖(AGENTS)인 것과 정합.
**근거:** 현재 수신자는 항상 보호자(`member_id`)라 병원 수신 경로가 0. **채팅(양방향)의 스키마 전제.**
**구현(#141, SA §4·§8-8 승격 반영):**
- `recipient_type`(MEMBER|HOSPITAL)+`recipient_id`를 정본으로 추가. 설계 판단 = **member_id 유지+일반화 병행** — `member_id`는 nullable로 완화해 기존 행 백필·하위호환용으로만 남기고(HOSPITAL 행은 NULL), 신규 로직·인덱스는 recipient_*를 쓴다(ddl-auto=update로 컬럼 drop이 위험하고 #134 데이터·경로 회귀를 피하기 위함).
- 마이그레이션은 이 저장소 관례(Flyway 없이 `schema_migrations` 마커 + `get_lock`)를 따른다 — 기존 행을 (MEMBER, member_id)로 백필 → recipient_* NOT NULL → member_id nullable 완화 → 인덱스를 `idx_notifications_recipient_created/read`로 교체. `NotificationRecipientMigrationRunner`.
- 발행부 무변경: `NotificationService.create(memberId,...)`는 `(MEMBER, memberId)` 위임 오버로드로 유지. 병원향은 `create(HOSPITAL, hospitalId,...)`.
- 조회·읽음·미읽음 수·모두 읽음(#134)을 호출자 recipient 기준으로 확장. 스태프→hospitalId는 기존 수단 `MemberService.getMyInfo`로 해석(`NotificationRecipientResolver`). 회원↔병원 격리.
- **범위 판단(사용자 확인) = SSE 병원 라우팅은 후속 PR로 분리.** #141은 저장·조회·인가 모델까지 포함하고, HOSPITAL 알림의 SSE 오배달만 방지했다(`SseNotificationPusher`가 MEMBER만 전송, HOSPITAL은 no-op).
- **후속 PR ① = 병원 수신 SSE 전달 경로(transport, 구현 완료).** `SseNotificationPusher.push(HOSPITAL, hospitalId, ...)`가 발송 시점의 현재 소속 스태프 memberId를 `MemberService.findActiveHospitalStaffMemberIds`로 받아 각 스태프의 모든 연결로 같은 payload를 전달한다. 저장은 병원 단위 1건·읽음 공유 그대로이고 fan-out은 전달 계층일 뿐이다 — 스태프별 알림 행·개별 읽음은 만들지 않는다. `SseEmitterRegistry` 키와 티켓 발급은 계속 개별 memberId이며(병원을 연결 키로 쓰지 않는다) 회원당 연결 상한 5도 그대로다. 탈퇴·소속 해제 스태프의 잔존 연결에는 보내지 않는다. 수신자 유형 해석은 exhaustive `switch` 식이라 `NotificationRecipientType`에 상수가 추가되면 컴파일 에러로 잡힌다(새 유형이 무전송으로 조용히 빠지는 것을 막는다). 대상별 전송은 개별 격리(`try/catch`)라 한 스태프의 전송 실패가 나머지 스태프의 전달을 삼키지 않는다. 프론트는 `StaffLayout`이 기존 `NotificationBell`을 그대로 재사용하고, 리소스 이동 경로는 벨이 로그인 회원 `role`로 직접 고른다(레이아웃 주입 없음 — 스태프가 보호자 `AppLayout` 화면에 서는 조합에서도 보호자 경로로 떨어지지 않는다).
- **후속 PR ② = 병원향 알림 발행 지점 (미착수, #166).** 위 ①은 **전달 경로만** 열었고, `NotificationService.create(HOSPITAL, ...)`를 호출하는 업무 발행 지점은 아직 없다 — 즉 병원 수신 알림은 실제 사용자 시나리오에서 아직 생성되지 않는다. **완료 조건:** ⓐ 병원 대상 발행 지점(예: 새 예약 요청 접수) 연결과 필요한 `NotificationType` 합의(위 "도메인 경계 조율"), ⓑ `members(hospital_id, role)` 조회 인덱스 — 현재 이 조회는 인덱스가 없어 `EXPLAIN` type=ALL(members 풀스캔)이고, 발행 지점이 없어 프로덕션 호출자도 없다. DDL이 STRICT 신호(AGENTS)이므로 인덱스는 이 저장소 관례(`schema_migrations` 마커 러너 + 엔티티 `@Index`)로 발행 지점 PR에서 함께 넣는다. ⓒ 발행 → 저장 → AFTER_COMMIT → 같은 병원 스태프 수신·타 병원 미수신 Level 6 E2E PASS.
- **알려진 한계(①의 범위 밖, 후속):** ⓐ 인메모리 레지스트리라 전달은 이 인스턴스에 연결된 스태프까지만 도달한다(단일 인스턴스 기준) — 다중 인스턴스 fan-out(Redis pub/sub)은 후속이고, 미도달분은 목록 폴링으로 복구된다. ⓑ 로그아웃·강제 세션 폐기 시 그 회원의 SSE 연결을 끊는 장치가 없어(티켓은 접속 시 1회만 검증) 최대 emitter 타임아웃 30분까지 연결이 살아 있다 — MEMBER 경로부터 있던 성질이지만 병원 수신이 열리면서 노출 범위가 병원 단위 알림으로 넓어졌다. ⓒ 전달은 커밋한 요청 스레드에서 동기 수행하므로 스태프 수 × 탭 수만큼 blocking write가 응답 시간에 올라탄다(`SseEmitterRegistry` heartbeat 주석의 한계와 같은 성질). ⓑ·ⓒ는 다중 인스턴스 확장 때 함께 해소한다.
- **스태프 알림 이동 경로:** 예약 리소스 알림도 병원 스태프는 **목록(`/staff/reservations`)**으로 보낸다. `StaffReservationDetailPage`가 목록에서 넘겨준 `location.state` 없이는 열리지 않기 때문(병원 예약 단건 조회 API가 SA §8-6에 없다) — 상세 URL로 보내면 즉시 목록으로 리다이렉트되는 죽은 링크가 된다. 단건 조회 API가 생기면 `NotificationBell`의 역할별 경로 표에 상세 함수를 채운다. 역할이 아직 확정되지 않은 동안(내 정보 조회 로딩·실패)에는 **어느 쪽 경로도 고르지 않고 이동을 보류**한다 — 보호자를 기본값으로 두면 그 창에 스태프가 알림을 눌렀을 때 보호자 경로로 샌다. `/staff/*` 안쪽은 `StaffRoute`가 역할 확인 전 레이아웃을 렌더하지 않아 안전하지만, 공개·보호자 라우트의 `AppLayout`은 인증만으로 벨을 렌더하므로 실제로 도달 가능한 경합이다(PR #162 리뷰 P2). 읽음 처리는 역할과 무관하므로 보류하지 않는다.
- 검증: Level 3 실제 MySQL — 백필·DDL·인덱스(`NotificationRecipientMigrationIntegrationTest`), 병원 단위 읽음 공유·회원↔병원 격리(`NotificationRecipientIntegrationTest`), 다중 스태프 동시 읽음 멱등(`NotificationMarkReadConcurrencyIntegrationTest`), #134 회원 회귀 없음. 후속 ① 전달 경로는 `NotificationHospitalFanOutIntegrationTest`(현재 소속만 수신·**연결별 payload 실제 도달**·커밋 후 전송·전송 실패 대상별 격리)와 `SseNotificationPusherTest`. 연결별 도달 검증은 emitter 미전송 버퍼를 읽어 확인한다 — 호출 횟수만 세면 "회원의 첫 연결에만 보내는" 구현도 통과하기 때문이다.

## 3.9 채널 확장(이메일) + 수신 설정 (고도화 / 정책)
**범위:** `NotificationChannel` 추상화(인앱 저장 · SSE(존재) · **이메일** 추가). 기존 `mail` 게이트웨이 재사용해 예약 승인·결제 결과 이메일 발송. 유형별 수신 on/off **스키마 선반영**.
**결정(기본):** 이메일은 예약승인·결제결과만, 수신설정은 스키마만 선반영(소급이 어려움). **WebSocket은 여기 아님**(채팅 전용).
**근거:** SSE는 앱이 연결돼 있을 때만 도달 → 앱을 닫으면 못 본다. 이메일이 그 공백을 메운다.

## 3.11 알림 저장 신뢰성 / 아웃박스 (홀, 낮음)
**범위:** 결제·예약 커밋과 같은 신뢰도로 저장 보장(같은 트랜잭션에 outbox row → 별도 발행자).
**근거:** SSE 실시간 유실은 **이미 폴링 fallback으로 커버**된다(저장이 원본). 남는 구멍은 `create()` **저장 자체가 실패**하면 보호자가 결과를 영영 못 받는 것뿐. 결제 [결제.md](결제.md) 3.6과 공유.

---

## 4. 의존성·순서
```text
3.8 미읽음/모두읽음 ─ 독립(지금 바로)
3.10 수신자 모델 ──→ 채팅.md(chat_messages·병원 수신)
3.9 이메일·수신설정 ─ 준독립
3.11 아웃박스 ─ 준독립(낮음)
```
권장: **3.8(지금)** → 3.10(채팅 전제) → 3.9 → 3.11.

## 5. 검증
- 3.8: 미읽음 수 정합성, read-all 멱등. 3.10: 병원 단위 읽음 공유, Level 3 MySQL 스키마.
- 3.11: "커밋 성공 + 발행자 다운" 재기동 시 정확히 1회 발행. 판정 PASS만 완료 근거.

## 6. 완료 기준
- 3.8·3.9·3.10·3.11 포함 범위 구현, 병원취소·대기열 등 새 NotificationType은 예약 도메인과 합의 후 정의.
- 수신자 모델이 SA §4 notifications에 반영된다. 제외 범위 미혼입, PR 리뷰 완료.
