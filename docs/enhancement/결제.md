# 고도화 — 결제 (담당: @maschemy)

> 이 문서는 동결된 MVP 정본에 대한 **델타(변경분)**다. 아래 `전제` 절에 적힌 SA·PRD 절만 함께 읽으면 되고, 그 밖의 MVP 문서는 읽지 않는다.
> 충돌 시: 이 문서가 `override 범위`에 선언한 것 안에서는 이 문서 > 동결 SA. 범위 밖은 동결 SA가 정본. (레이어 규칙: [README](README.md))

## 메타

- **상태:** 초안
- **담당:** @maschemy
- **관련 이슈:** #
- **전제(동결 baseline — 이 절만 함께 읽는다):** SA §9-4(멱등·실패 분기), SA §5-2(결제 상태), SA §8-7(오프라인 정산), SA §4 payments·payment_methods, PRD §6-7
- **override 범위(이 문서가 바꾸는 계약):** ① 청구 금액 항목화(payment_items 신설), ② 영수증 발급, ③ 결제수단 기본값·예약 결제수단 재지정, ④ 부분 환불 + 정정 재청구(payments의 예약당 활성 결제 UNIQUE 완화), ⑤ 결제 실패 셀프 복구, ⑥ 결제 알림 내용(금액·PENDING 안내)
- **건드리지 않는 것:** 청구·환불·정산·오프라인·웹훅의 **발행 지점·중복 차단(`applied`)** 구조 유지. 실패 분기·재시도·정산 스케줄러 유지. 알림 **전달 인프라**는 [알림.md](알림.md) 소유. 현장 수납분 환불은 제외(확정).

## 확정 결정 변경 (팀 합의 완료)
- develop AGENTS "확정된 핵심 결정"의 **"환불한 예약은 재청구할 수 없다(SA §9-4 알려진 한계)"**는 정정 재청구(3.5)로 해제한다 — **팀 합의 완료**.
- 남은 절차: **P5 구현 착수 시 AGENTS·SA의 해당 확정 결정 줄을 함께 갱신**한다(그 갱신은 동결 대상 SA 편집이므로 P5 구현 PR에 포함). 그 전까지 이 문서가 근거 정본이다.

---

## 3.1 청구 금액 항목화 — 고도화 (STRICT: 스키마)
**범위:** `Payment.amount` 단일 총액 → `payment_items`(명칭·수량·단가·금액) 리스트. 총액 = 항목 합계 정합. 청구 시점 스냅샷으로 고정. 청구/환불/영수증/정정 근거로 재사용.
**근거:** 총액 하나라 "왜 8만원인지" 알 수 없고, 환불은 이미 있는데 분쟁 근거 데이터가 없다. **3.4·3.5의 데이터 전제**라 결제 계열 선두.
**제외:** 세율·부가세 분리, 진료 항목 마스터 코드 표준화.

## 3.4 영수증·증빙 발급 — 신규 (JSON)
**범위:** 결제 완료 건 영수증 데이터(항목·총액·병원·일시·결제수단 brand·last4), 펫보험 청구용 최소 증빙 필드. **형식은 구조화 JSON까지**, PDF/전자문서는 후속.
**근거:** 지금 내려줄 게 `PaymentHistoryResponse`뿐. 펫보험 청구에 영수증이 필요.
**제외:** 세금계산서·현금영수증 국세청 연동, 진료확인서(수의사 서명).

## 3.2 결제수단 기본값·예약 결제수단 재지정 — 고도화
**범위:** 기본 결제수단 지정·변경 API, **예약의 결제수단 재지정**(진료 전 카드 교체 반영), DELETED 결제수단이 예약에 물린 경우 처리.
**근거:** `Reservation.paymentMethodId`가 요청 시점 고정 → 카드 교체 시 기존 카드 DELETED → 진료 후 청구가 전부 `OFFLINE_REQUIRED`로 떨어진다. 재지정 수단이 없다.
**제외:** 우선순위 자동 폴백 체인, 분할 결제.

## 3.3 결제 실패 셀프 복구 + 3.5 부분 환불·정정 재청구 — **STRICT 클라이맥스**
**범위:**
- 3.3: `OFFLINE_REQUIRED`에서 보호자가 카드 교체 후 **재청구 트리거**하는 셀프 경로.
- 3.5: 부분 환불, 그리고 금액 정정(8만→6만) = 기존 PAID를 REFUNDED로 돌리고 **새 payment row 생성 + `correction_of`(이전 payment_id) 체인**.
- 3.3·3.5는 **같은 "새 시도 = 새 row + 체인" 메커니즘 공유**.

**활성 결제 유일성(확정 방식):** MySQL은 부분 유니크 인덱스가 없으므로, `payments`에 nullable **생성 컬럼 `active_reservation_id`**(활성일 때만 `reservation_id`, 아니면 NULL) + `UNIQUE(active_reservation_id)`. NULL은 UNIQUE에서 중복이 아니므로 "예약당 활성 결제 1건"이 DB 제약으로 못박힌다.
**동시성(STRICT):** 활성 결제가 있으면 새 청구를 막는 조건부 처리 → 이중결제 방지. 다중 스레드 "1건만 성립" 테스트 필수(SA §9-4 멱등).
**구현 시 확정할 세부:** "활성"의 정확한 술어 — status만으로 부족(OFFLINE_REQUIRED도 재청구 전엔 활성)하므로 명시적 `superseded_at IS NULL` 마커가 필요.
**근거:** 전액 환불만 가능해 정정하려면 전액 환불 후 신규 청구인데, `reservation_id` UNIQUE가 같은 예약 재청구를 막아 금액 오타 하나가 오프라인 수납으로 강제 전환된다.
**제외(확정):** 현장 수납분 환불, 자동 재시도 스케줄.

## 3.6 결제 알림 내용 보강 — 홀 메꾸기
**범위:** P2 결제 알림에 금액 포함(현재 문구만, 금액 없음). P1 `PENDING`/`RECONCILE_STUCK` 무음 해소 — "결제 확인 중" 안내 1회(상태는 안 바꾸고 안내만, 이중수납 금지 유지).
**근거:** 푸시/배지에 금액이 있어야 쓸모, PENDING 영구 무음은 CS 직행. 실시간은 이미 SSE로 도달하므로(develop) 여기서는 **content(금액)·PENDING 안내만** 보강하면 된다.
**의존:** 저장 신뢰성(아웃박스)은 [알림.md](알림.md) 3.11과 공유.

---

## 4. 의존성·순서
```text
3.1 항목화 ─→ 3.4 영수증
          └─→ 3.3+3.5 (정정 재청구, 활성결제 UNIQUE)
3.2 결제수단 재지정 ─ 준독립
3.6 알림 내용 ─ 알림.md 인프라 의존
```
권장: 3.1 → 3.4 → 3.2 → 3.3+3.5(STRICT 클라이맥스). 3.6은 알림 인프라와 맞춰 진행.

## 5. 검증 (STRICT: 상태·멱등·동시성·스키마)
- 3.5/3.3: 동시 재청구 이중결제 방지, 활성결제 UNIQUE, 정정 체인 정합성을 다중 스레드 + Level 3 MySQL로 검증.
- 3.1: 항목 합계 = 총액. 3.6: 알림 안내 추가가 상태 전이·이중수납에 영향 없음.
- 판정 PASS만 완료 근거. Mock PASS와 실제 DB·PG 검증 구분.

## 6. 완료 기준
- 3.1·3.2·3.3·3.4·3.5·3.6 포함 범위 구현, 부분환불·정정재청구 근거가 SA §5-2·§9-4·§4 payments에 반영.
- 활성결제 UNIQUE·정정 체인 스키마가 MySQL Level 3 통과, 이중결제 방지 동시성 테스트 통과.
- 제외 범위(현장 수납 환불 등) 미혼입, PR 리뷰 완료.
