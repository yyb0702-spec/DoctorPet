# 고도화 — 병원 (담당: @sirisiyesiri)

> 이 문서는 동결된 MVP 정본에 대한 **델타(변경분)**다. 아래 `전제` 절에 적힌 SA·PRD 절만 함께 읽으면 되고, 그 밖의 MVP 문서는 읽지 않는다.
> 충돌 시: 이 문서가 `override 범위`에 선언한 것 안에서는 이 문서 > 동결 SA. 범위 밖은 동결 SA가 정본. (레이어 규칙: [README](README.md))

## 메타

- **상태:** 초안
- **담당:** @sirisiyesiri
- **관련 이슈:** #113 ~ #119
- **전제(동결 baseline — 이 절만 함께 읽는다):** SA §8-3(병원 검색)·§9-1(QueryDSL)·§9-2(Redis 캐시), SA §9-6(공공데이터 배치·2계층 매핑), SA §9-5(AiGateway·Tool Calling), PRD §7(AI 안전 정책), SA §4 hospitals·hospital_details·hospital_capabilities·ai_consultations·pet_profiles
- **override 범위(이 문서가 바꾸는 계약):** 병원 찜/리뷰/평점(신규), 병원 직접 입력 진료시간·진료역량, 최초 제휴 시드와 주간 공공데이터 갱신의 책임 분리, AI 상담의 펫 프로필 연동, 진료역량·리뷰 반영 AI 추천, 예약 응답률·평균 승인 시간 지표
- **건드리지 않는 것:** 기존 검색의 거리·영업상태·진료역량 필터 계약, 30분 고정·슬롯당 1건 정책, AI 안전 정책(PRD §7), 인증·인가 원칙. 결제·알림·채팅 도메인과 겹치지 않는다.

## 대상 기능 (7)
1. **병원 찜하기(#113) — develop 완료, 작업 아님** 2. **리뷰 CRUD·평점 집계(#114) — develop 완료, 작업 아님** 3. 진료시간 관리(#115) 4. 진료역량 관리(#116) 5. AI 상담↔펫 프로필 연동(#117) 6. 진료역량·리뷰 반영 AI 추천(#118) 7. 예약 응답률·평균 승인 시간(#119)

기존 검색·상세·예약·AI 상담 흐름을 대체하지 않는다. 병원 운영 정보 정확도와 선택 근거·개인화 추천을 보완하는 후속 고도화다.

## 공통 구현 원칙
- 회원·병원 스태프는 인증 주체로 식별(요청 body의 memberId·hospitalId 신뢰 금지). 스태프는 자병원 정보만, 회원은 본인 리뷰·찜만 수정·삭제.
- 공공데이터는 병원명·주소·전화번호·좌표·영업 상태(`business_status`) 등 원천 기본정보만 갱신한다. 진료역량을 제공하거나 추론하지 않으며, 제휴 상세·진료역량·운영 스케줄·임시 휴무·예약 데이터는 변경하지 않는다.
- AI 추천은 실제 저장된 병원·진료역량·리뷰만 사용하고, 없는 리뷰·평점·진료역량을 생성하지 않도록 입출력을 검증한다.
- 리뷰·찜·통계 없는 병원이 검색·추천에서 부당하게 완전 제외되지 않게 한다. 스키마·인덱스 변경은 실제 MySQL Level 3 검증.

---

## 3.1 병원 찜하기 — #113 (develop 완료, 이 문서 작업 대상 아님)
**PR #128로 merge·develop 반영 완료.** 이 문서를 쓸 당시엔 미착수였으나 그사이 구현이 끝났다 — 아래는 실제 정본(코드)이 확정한 내용이고, 더 이상 "선행 정책 결정"이 아니다.
- **API:** `PUT`/`DELETE /api/hospitals/{hospitalId}/favorite`(등록/해제), `GET /api/members/me/favorite-hospitals`(내 목록). GUARDIAN 전용(`SecurityConfig`에서 `hasRole("GUARDIAN")`, 비인증 401·스태프 403).
- **중복·멱등:** `(member_id, hospital_id)` UNIQUE + 네이티브 `INSERT ... ON DUPLICATE KEY UPDATE`로 동시 중복 등록도 1건만 성립(8스레드 동시 요청 통합 테스트로 검증됨). 해제는 대상이 없어도 무조건 204.
- **응답 필드명은 `favorite`**(이 문서가 가정했던 `isFavorite`이 아니다) — 검색(`HospitalSearchResponse`)·상세(`HospitalDetailResponse`) **둘 다**에 `boolean favorite` 반영됨. 비인증·병원 스태프는 항상 `favorite=false`(정책 확정).
- **캐시 분리:** 공유 Redis 검색 캐시에는 회원별 `favorite`이 들어가지 않는다. 캐시 HIT/MISS 어느 쪽이든 응답 직전에 병원 ID 배치 조회 1회로 결합(N+1 없음, 다른 회원 간 찜 상태 섞임 없음 — 테스트로 확인).
- **내 찜 목록 정렬:** `createdAt DESC, id DESC`(안정 정렬). **폐업 병원도 영업상태와 함께 그대로 노출**(정책 확정).
- **회원 탈퇴:** 탈퇴 회원의 찜만 cascade 삭제, 다른 회원 찜은 보존.
**남은 것:** 찜 수의 검색 정렬·AI 추천 반영, 인기 순위, 찜 기반 알림, 병원 비교 화면 — 이 문서 범위 밖으로 계속 제외.

## 3.2 병원 리뷰 CRUD 및 평점 집계 — #114 (develop 완료, 이 문서 작업 대상 아님)
**PR #129로 merge·develop 반영 완료.** 이 문서를 쓸 당시엔 미착수였으나 그사이 구현이 끝났다 — 아래는 실제 정본(코드)이 확정한 내용이고, 더 이상 "선행 정책 결정"이 아니다.
- **작성 자격:** 본인 예약의 결제 상태가 `PAID`/`OFFLINE_PAID`일 때만. **예약 1건당 최초 1회**(`Reservation.reviewedAt` 조건부 UPDATE로 원자적 선점, 동시 작성 테스트로 검증됨).
- **평점 범위:** 1.0~5.0, 0.5 단위(앱 검증 + DB `CHECK` 제약 둘 다).
- **삭제 방식:** **Hard Delete**(Soft Delete 아님). 사용자 직접 삭제는 `reviewedAt`을 유지해 재작성을 차단하고, **환불 시에는** 리뷰를 Hard Delete하면서 `reviewedAt`도 초기화해 재작성을 허용한다(환불·리뷰 CRUD 경합은 결제 행/리뷰 행 `PESSIMISTIC_WRITE` 락으로 직렬화).
- **신고·숨김:** 포함 안 함(제외 확정).
- 병원 **상세** 응답에 `averageRating`(리뷰 없으면 null)·`reviewCount`(0) 반영됨. **검색 목록 응답에는 반영되지 않음**(상세 전용) — §5 참고.
- 별도 리뷰 상세 조회 API는 없음(목록 응답이 공개 정보를 전부 제공).
**남은 것(#118 의존):** 리뷰의 AI 추천 반영(#118), 리뷰 기반 검색 랭킹, 찜·응답률 종합 점수, 대규모 신고·제재, 작성 보상, 이미지·동영상 리뷰 — 이 문서 범위 밖으로 계속 제외.

## 3.3 병원 스태프 진료시간 관리 — #115
**선행 정책 결정:** 요일별 휴게·점심 표현, 변경 적용일, 미래 슬롯 재생성 범위, 예약 포함 슬롯 보존 방식, 정기/임시 휴무 구분, 공휴일 예외.
**범위:** 자병원 진료시간 조회·수정, 요일별 시작·종료·휴게시간 관리, 잘못된/중첩 시간 검증, 자병원·역할 권한 검증, 예약 없는 미래 슬롯 안전 재생성, 예약 있는 슬롯 보호, 기존 close-day·임시 휴무 연동.
**흐름:** 변경 요청 → 입력값·권한 검증 → 기존 예약 슬롯 보호 → 예약 없는 미래 슬롯 정리 → 변경 시간 기준 재생성.
**근거:** `SlotGenerationService`가 `HospitalDetail.openHours`로 슬롯을 만들지만 수정 API가 없다. 점심·공휴일·계절 변경 미반영 시 실제 운영시간과 슬롯 불일치, close-day만으로는 부분 변경 불가, 기존 예약 보호하며 미래만 재생성해야 유실 없음.
**제외:** 진료 항목별 가변 슬롯, 수의사별 일정·동시 수용, 슬롯당 복수 예약, 30분 정책 변경, 시작 시각 엄격 실시간 보장.

## 3.4 병원 스태프 진료역량 관리 — #116
**확정 정책:** 공공데이터에는 진료역량이 없고 서버도 공공데이터에서 진료역량을 추론하지 않는다. `partner-hospitals.json`의 역량은 최초 제휴 데이터 구축을 위한 초기값일 뿐이며, 이후에는 담당 병원 스태프가 저장한 `hospital_capabilities` 현재값을 최종값으로 사용한다. 별도 출처 컬럼·수정 이력·관리자 승인 절차는 두지 않는다.

**범위:**
- 인증된 `HOSPITAL_STAFF`가 `@AuthenticationPrincipal`의 `memberId`로 확인된 자병원의 진료역량을 조회하고 전체 교체한다. 요청 body·query·path의 `memberId`·`hospitalId`는 식별 근거로 사용하지 않는다.
- 기존 `CapabilityValue` 19개 화이트리스트만 허용하고, 값이 가진 `CapabilityType`을 그대로 사용한다. 허용되지 않은 값과 요청 내 중복 값은 400으로 거부한다.
- 수정 요청은 추가·삭제 명령이 아니라 전달받은 전체 목록으로 현재 진료역량을 교체한다. 결과는 승인 대기 없이 즉시 병원 상세와 QueryDSL 검색 필터에 동일하게 반영한다.
- 공공데이터에서 `business_status=CLOSED`로 확인된 병원은 진료역량을 수정할 수 없다. 폐업해도 제휴 상태, 상세정보, 진료역량과 과거 예약·결제·리뷰 이력은 삭제하지 않는다.

**제휴 시드·주간 갱신:**
- 최초 데이터 구축에서는 공공데이터 병원 적재 후 `partner-hospitals.json`을 한 번 적용해 제휴 상태·상세정보·초기 진료역량을 생성한다.
- 주간 공공데이터 갱신은 공공 원천 기본정보와 `business_status`만 갱신하고 검색 캐시를 무효화한다. 제휴 JSON을 재적용하지 않으며 `partnership_status`, `hospital_details`, `hospital_capabilities`, 운영 스케줄, 임시 휴무와 예약 데이터는 변경하지 않는다.
- 폐업은 제휴 계약 종료와 다른 개념이므로 `business_status=CLOSED`만으로 `partnership_status`를 자동 변경하지 않는다. 폐업 병원은 일반 검색·신규 예약·신규 슬롯 생성에서 제외하고, 기존 예약과 `RESERVED` 슬롯은 자동 삭제·이동·취소하지 않는다.

**흐름:** 최초 제휴 시드로 초기 역량 생성 → 담당 병원 스태프가 전체 목록 조회·교체 → `hospital_capabilities` 현재값을 상세·검색에서 즉시 사용. 주간 공공데이터 갱신은 이 흐름과 분리한다.

**근거:** 진료역량은 공공데이터 원천 필드가 아니며 실제 지원 동물·검사·전문 진료·장비는 담당 병원이 가장 정확하게 관리할 수 있다. 현재 주간 갱신이 제휴 JSON을 재적용하는 구조는 운영자가 수정한 값을 초기 시드로 되돌릴 수 있으므로, 최초 구축과 주기 갱신의 책임을 분리한다.

**제외:** 출처 구분 컬럼, 진료역량 수정 이력, 관리자 승인 시스템, 자격증 증빙, 수의사 개인별 진료역량, 항목별 가격, 진료역량별 슬롯 길이 변경.

## 3.5 AI 상담과 회원 펫 프로필 연동 — #117
**범위:** 상담 요청에서 회원 소유 펫 선택·소유권 검증, 삭제·접근 불가 펫 차단, 축종·품종·나이·성별·중성화 등 필요한 정보만 추출·문맥 반영, 펫 미선택 기존 흐름 유지, 사용한 펫 식별을 위한 연관/스냅샷 정책 검토, AI 전달 개인정보 최소화.
**흐름:** 인증 회원 → 소유 펫 선택 → 소유권·삭제 검증 → 필요한 프로필만 추출 → 증상 + 프로필로 상담.
**근거:** 등록된 펫 정보가 상담에 연결 안 돼 반복 입력, 같은 증상도 축종·나이·중성화로 문맥·추천 진료역량이 달라짐, 반복 입력 감소·정확도 향상, 타 회원 펫 차단 위해 소유권 검증.
**제외:** 리뷰·진료역량 기반 최종 추천(#118), 펫 의료기록 자동 분석, 진단·처방 생성, 삭제된 펫의 신규 상담 사용.

## 3.6 진료역량과 리뷰를 반영한 AI 병원 추천 — #118
**선행 이슈:** #114·#116·#117.
**범위:** AI가 파악한 증상↔병원 진료역량 연결, 선택 펫 축종↔병원 지원 동물 비교, 거리·영업상태를 기본 후보 조건 유지, 평균 평점·리뷰 수·선별 리뷰를 근거로 반영, 병원별 추천 이유·객관적 근거 제공, 리뷰/진료역량 부족 병원 fallback, 실제 저장 데이터만 사용 검증, AI 실패 시 기존 안전 응답·검색 fallback 유지.
**흐름:** 증상 + 펫 프로필 → 필요한 진료역량 도출 → 거리·영업상태로 후보 제한 → 지원 동물·진료역량 일치 확인 → 평점·리뷰 수·선별 리뷰로 보완 → 추천 병원 + 이유 + 근거.
**근거:** 거리·영업상태만으로는 진료 가능성·경험 설명 부족, 직접 확인 진료역량이 추론보다 정확, 펫↔지원 동물 비교로 부적합 병원 감소, 평점+리뷰 수로 소수 고평점 과대평가 완화, 근거 동반 제공으로 AI의 근거 없는 설명 방지.
**제외:** 전체 검색 종합 랭킹, 찜 수·응답률 자동 반영, 광고·제휴 순위, AI 자동 예약, 의료 진단·처방.

## 3.7 병원 예약 응답률 및 평균 승인 시간 — #119
**선행 정책 결정:** 응답률 분모·분자, 승인/거절/승인만료/회원취소 집계 기준, 집계 기간, 최소 표본 수, 표본 부족 시 노출 방식, 대표값(평균/중앙값).
**범위:** 병원별 응답률·요청→승인 평균 소요시간 집계(기존 requestedAt·confirmedAt 활용), 상세 응답 반영, 집계 기간·표본 수 병기, 데이터 없음·표본 부족 처리, 집계 쿼리 인덱스·성능 검증.
**흐름:** 병원별 예약 이력 → 확정 상태·기간·표본 정책 적용 → 응답률 + 평균 승인 시간 + 표본 수 → 병원 상세.
**근거:** 승인형 예약이라 응답 여부·속도를 보호자가 알 필요, 필요한 데이터가 이미 존재, 리뷰·평점만으론 운영 품질 판단 어려움, 표본 적은 수치 노출은 오해 유발 → 최소 표본 정책 필요.
**제외:** 응답률의 검색 정렬·AI 추천 반영, 스태프별 응답률, 실시간 순위, 성과 평가·제재, 운영 대시보드 전면 구축.

---

## 4. 구현 의존성·순서
```text
진료시간(#115) → 운영시간·슬롯 정합성
진료역량(#116) ─┐
리뷰·평점(#114, develop 완료) ─┼→ AI 추천(#118)
펫 프로필(#117) ─┘
찜(#113, develop 완료) → 재탐색 편의    응답률·승인시간(#119) → 선택 품질 정보
```
권장 순서: #115 → #116 → #117 → #119 → #118. (#113·#114는 이미 완료라 순서에서 제외 — #118 착수 시점에 이미 준비돼 있는 데이터로 취급한다.) AI 추천(#118)은 리뷰·진료역량·펫 프로필 데이터가 준비된 뒤 — 데이터 관리 없이 추천부터 만들면 근거 부족·환각 위험.

## 5. 기존 검색 성능 개선(#69)과의 관계
#69(검색 후처리 조회 범위 개선: 전체 메모리 적재 회피, 무좌표 병원 거리 정렬 경계, 영업중 후처리 배치화, 페이지 계약 유지)는 본 고도화와 별도로 완료했다. 리뷰(#114)의 `averageRating`·`reviewCount`는 **병원 상세 응답에만** 추가됐고 검색 목록 응답에는 없어 #69의 검색 페이지네이션과 무관하다(코드 확인). 찜(#113, PR #128, develop 완료)은 검색 목록에도 `favorite` 필드를 추가했지만 **재검증 결과 문제없음** — `HospitalService`의 찜 결합(`withFavorites`)은 캐시·페이지네이션·배치조회로 이미 결정된 결과 목록에 대해 병원 ID 배치 조회 1회를 얹는 순수 후처리 단계라, #69가 확정한 오프셋/배치 크기/페이지 계약 자체는 건드리지 않는다(코드 확인, `HospitalSearchServiceTest`의 캐시 HIT/MISS 테스트로도 검증됨). 응답률(#119)이 검색 목록에 필드를 추가하게 되면 동일한 방식(사후 결합, 페이지네이션 로직 불변)으로 재검증한다. #70은 재검토 후 종료, 범위에서 제외.

## 6. 검증
- 기능/권한/정합성: ~~찜 중복·해제, favorite 일치~~(#113), ~~리뷰 CRUD·자격·중복 차단, 평점·리뷰 수 집계 정합성~~(#114) — 둘 다 develop에서 이미 검증 완료, 이 문서 범위 아님. 진료시간 변경 후 미래 슬롯 재생성·기존 예약 보호, 자병원 역량 전체 교체·화이트리스트·중복·폐업·타 병원 수정 차단, 주간 공공데이터 갱신 시 제휴 JSON 미재적용과 역량 보존, 상세·검색 결과 일치, 소유 펫만 상담·타인/삭제 펫 차단, 실제 데이터만 사용한 추천·이유 일치, 응답률·평균 승인 시간 집계.
- 실행: Level 1 전체 Gradle, Level 2 응답·Validation·권한, Level 3 MySQL(UNIQUE·집계·스키마·동시성), Level 5 로컬 기동, Level 6 실 HTTP. AI 추천은 외부 실연동과 Fake Gateway 결과를 구분 기록. 검색·상세 필드 추가 시 캐시 키·무효화·쿼리 성능 함께 확인.

## 7. 이번 고도화 제외
30분 슬롯 정책 변경, 항목별 가변 슬롯, 수의사별 일정·동시 수용, 슬롯당 복수 예약, 시각 엄격 실시간 보장, 찜·리뷰·응답률 종합 검색 랭킹, 찜·응답률의 AI 추천 자동 반영, 광고·제휴 순위, 리뷰 이미지·동영상, 리뷰 보상·본격 신고/제재, 운영 통계·관리자 대시보드, AI 진단·처방, AI 자동 예약, 새 외부 알림 채널.

## 8. 완료 기준
- #115~#119 확정 범위 구현(#113·#114는 develop 완료로 제외), 선행 정책은 구현 전 PRD·SA 반영, 인증 주체 기반 권한·소유권 검증, 정합성·MySQL 스키마·인덱스·집계 검증 통과, AI 추천이 실제 데이터만 사용·AI 실패 시 안전 정책·fallback 유지, Level 5·6 결과를 verification-guide 양식 기록, PRD·SA·API·경량 문서 반영, 제외 범위 미혼입, PR 리뷰 완료.
