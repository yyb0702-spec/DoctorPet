# 전체 SpringBootTest가 외부 AI 키 없이도 공통 컨텍스트를 구성하도록 테스트 전용 Fake Gateway를 사용한다.
spring:
  # 테스트 리소스가 main application.yaml을 가리므로 JPA가 엔티티 스키마를 생성하도록 명시한다.
  jpa:
    hibernate:
      ddl-auto: update

ai:
  gateway: fake
